Repair the damaged 2025 Lattice Occupational Pensions XBRL taxonomy and produce a self-contained, offline-valid XBRL Taxonomy Package at `/app/out/repaired-taxonomy.zip`.

Use `/app/current-package`, `/app/prior-package`, the desk evidence (`/app/desk/change-memo.txt`, `/app/desk/line-item-cubes.csv`, `/app/desk/marked-contexts.json`, `/app/desk/operating-notes.txt`, `/app/desk/migration-logs.txt`), and `filing-gate` (on PATH while you solve, not available at grading time) to reconstruct the intended 2025 model. Some differences between 2024 and 2025 are intentional: the 2025 desk evidence describes the approved changes, prior-year material covers mechanics that remain unchanged, and damaged material in the current 2025 package should be treated as untrusted.

Write `repaired-taxonomy.zip` as a real ZIP file, not a symlink, containing exactly one top-level package directory (call it `<package-root>`). Inside it, include `<package-root>/META-INF/taxonomyPackage.xml` in namespace `http://xbrl.org/2016/taxonomy-package` with identifier `http://lattice.example/lopt/taxonomy-package/2025` and an entry point whose href resolves to the 2025 entry schema; `<package-root>/META-INF/catalog.xml` in namespace `urn:oasis:names:tc:entity:xmlns:xml:catalog`, remapping the 2025 taxonomy URIs and the required standard XBRL schema URIs onto files inside the package so DTS resolution needs no network; and `<package-root>/taxonomy/lopt-2025.xsd` as the 2025 entry schema. Keep both 2025 namespaces, `http://lattice.example/lopt/2025-01-31` and `http://lattice.example/lopt/ext/2025-01-31`, include the remaining schemas, linkbases, and resources the DTS requires, and repair the schema, import, and package-resolution defects along with the dimensional relationships.

Preserve every concept declaration already present in the supplied 2025 taxonomy schemas unless this task explicitly requires that declaration removed — don't prune a declaration just because the repaired model doesn't use it, and withdrawing a concept from a member tree doesn't delete its schema declaration. That includes `CubeLegacy`, `ValuationDimension`, `ValuationDomain`, `Ongoing`, and `Windup`, plus the concepts the model actually uses: NetAssets, ChangeInNetAssets, Contributions, EmployerContributions, BenefitsPaid, InvestmentReturn, AdminExpenses, HybridGroup, Dependant, `lopt:Other`, and `ext:Other`. A preserved declaration doesn't preserve obsolete relationships or require new ones. `lopt:Other` stays declared but withdrawn from the geography member tree; Dependant stays declared for leftover tagging and outside the usable MemberClass members; the legacy and valuation concepts stay declared without new cube attachments. NetAssets has an instant period type; ChangeInNetAssets, Contributions, EmployerContributions, BenefitsPaid, InvestmentReturn, and AdminExpenses are duration.

Every effective has-hypercube relationship must explicitly carry `xbrldt:closed` — `true` or the XML Schema Boolean `1` for closed cubes and exclusions, `false` or `0` for open ones. Closed positive cubes reject extra dimensions on their context element; open positive cubes permit them; open `notAll` exclusions still match when additional dimensions appear on the same context side.

NetAssets gets a closed CubeScheme on segment with scheme members only, Occupational as the default; HybridGroup is non-usable, with CashBalance and MixedBenefit as reportable members beneath it. ChangeInNetAssets and its movement descendants inherit an open CubeFlow on segment across the scheme, geography, and member class dimensions — Domestic is the geography default, Overseas is a non-usable grouping heading, there's no MemberClass default, the usable MemberClass members are Active, Deferred, and Pensioner, and core geography `lopt:Other` is withdrawn from the geography member tree. Contributions and EmployerContributions get the open CubeForbiddenContributions `notAll` exclusion on scenario for Pensioner, and extra dimensions on that same context side don't disable it. BenefitsPaid gets the open CubeBenefitsExclusion `notAll` exclusion on segment for Personal plus Rest of World, same deal — extra flow dimensions on that side don't disable it. AdminExpenses also gets a closed CubeAdmin on scenario, PresentationCurrency as the default, with SettlementCurrency and `ext:Other` as usable members; `ext:Other` is the only valid currency Other, so don't treat `lopt:Other` as a currency member. Dependant stays declared in the schema for leftover tagging and isn't a usable MemberClass member.

The completed package has to load entirely offline, use its catalog for canonical URI resolution, and have no dependency on files outside the ZIP. It needs to represent the required effective 2025 dimensional model, permit the required valid filings, and reject the required invalid filings, including filings the desk hasn't marked. Valid equivalent serializations and modular DTS organizations are fine.
